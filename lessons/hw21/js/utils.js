export const makePageElementVisible = (el) => el.classList.remove('invisible')
export const makePageElementNotVisible = (el) => el.classList.add('invisible')

export const disableElement = (el) => el.classList.add('disabled')